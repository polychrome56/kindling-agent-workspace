---
description: kindling C/C++ 工程实践规则（按路径生效）
globs: kindling/**/*.{c,cc,cpp,cxx,h,hh,hpp}
alwaysApply: false
---

# Kindling C/C++ 工程实践

- 回复用户时使用中文；代码标识符、命令、API 名称保持英文。
- 不要把既有实现自动视为最佳实践；发现裸资源管理、全局可变状态、错误被吞、接口职责混乱、难以测试等问题时，应直接指出并给出小步改进建议。
- 新增代码默认采用清晰的小函数、小类型和显式依赖；避免长函数、隐式全局状态、复杂继承层级、宏技巧和预防式抽象。
- 优先级：正确性与可测试性 > 资源生命周期 > 清晰接口 > 简单设计 > 性能；只有证据充分时才牺牲可读性换性能。
- 所有权必须显式：栈对象优先，独占所有权用 `std::unique_ptr`，共享所有权必须说明原因；禁止新增裸 `new/delete`。
- 资源管理默认 RAII 和 Rule of Zero；直接管理 fd、socket、netlink、BPF/tc、namespace、线程、锁等资源时，必须设计清楚构造、析构、移动和失败路径。
- 公共接口必须能看出失败语义、所有权、生命周期和线程安全；避免模糊的全局状态、隐式副作用和隐藏依赖。
- 设计模式用于解决真实变化点，不用于装饰代码；优先组合、Dependency Injection、Strategy、Factory、Adapter、PIMPL，谨慎使用 Singleton 和深继承。
- 系统边界代码必须保留错误信息和诊断上下文；不要吞掉 `errno`、返回码或关键日志。
- 热路径避免不必要堆分配、异常、虚派发、字符串拼接和高频日志；性能判断需要 profile、benchmark 或可复现实验。
- 实质修改后优先补最小测试或验证命令；无法验证时必须说明原因、风险和建议的后续验证方式。
